# Roadmap

## Now

- Use audit-kit on 2 more real repos (one non-Next backend, one messy React/Next app)
- Cut `v0.1.1` if a packaged release is needed (do not move `v0.1.0`)

## Next

- Direct `spawn` refactor for rg/shell commands (drop `sh -c` where possible)
- Ledger write lock if concurrent agents become a real workflow
- Scan timing on large repos before parallelizing rg

## Later

- Stack-aware boosts for Rails / Laravel / etc.
- Report polish / client-ready export
- Optional runbook command
